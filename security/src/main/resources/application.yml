spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:34.22.93.234}:${MYSQL_PORT:3306}/${MYSQL_DB_NAME:security}
    username: ${MYSQL_USERNAME:nhnuser}
    password: ${MYSQL_PASSWORD:1234}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    # DB에 테이블 없으면 자동생성
    hibernate:
      ddl-auto: update
  jackson:
    property-naming-strategy: SNAKE_CASE

  data:
    redis: #현재 환경변수 치환 실패 문제로 common package 내 RedisConfig 로 Bean 주입중 (즉, 아래 설정 무효화됨)
      host: ${REDIS_HOST:34.22.93.234}
      port: ${REDIS_PORT:6379}

server:
  port: 8080

exchange-server:
  host: ${EXCHANGE_SERVER_HOST:localhost}
  port: ${EXCHANGE_SERVER_PORT:8081}

jwt:
  secret-key: ${JWT_SECRET_KEY:dsacsawdwqsadsdascsacsaccsac123456789012345678901234567890}
  access-token-expired-at: ${ACCESS_TOKEN_EXPIRED_AT:5}
  refresh-token-expired-at: ${REFRESH_TOKEN_EXPIRED_AT:7}

# secret key는 임시값 Byte 수 조절
aes:
  secret-key: ${AES_SECRET_KEY:my-super-secret-key-1234}